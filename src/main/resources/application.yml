server:
  port: 8081
spring:
  datasource:
    password: Rishi@449
    url: jdbc:mysql://localhost:3306/jrtp26
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    host: smtp.gmail.com
    password: ucqp iaef zsnr qscm
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    username: nneelampraveen@gmail.com
    
app:
 messages:
   dupEmail: Duplicate Email
   regSucc: User Registered 
   regFail: Registration Failed
   invalidCredentials: Invalid Credentials
   pwdMatchErr: New Pwd and Old Pwd should be same
   pwdUpdateErr: Password Update Failed
   oldPwdErr: Given Old Pwd is Wrong

